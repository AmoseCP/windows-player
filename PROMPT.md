# Windows 音乐播放器 — Claude Code 开发提示词

> 使用方法:把下面整段内容作为第一条指令发给 Claude Code。建议先 `cd` 到一个空目录再开始。

---

请为我开发一个 Windows 桌面音乐播放器,以下是完整需求,请严格按照需求实现。

## 一、项目概述与技术栈

- **平台**:Windows 10/11 桌面应用
- **技术栈**:Electron + React + Vite(渲染进程),主进程用 Node.js
- **播放引擎**:使用 HTML5 `<audio>` 元素播放。Chromium 内置解码器可直接播放 MP3、WAV、FLAC、OGG、AAC、M4A、MP4 等格式。**播放 MP4 文件时只播放其音轨,不显示、不渲染任何视频画面**(用 audio 标签加载 mp4 即可自动实现)
- **元数据解析**:使用 `music-metadata` npm 包读取歌曲的标题、艺术家、专辑、时长和内嵌封面图
- **拖拽排序**:使用 `@dnd-kit/core` + `@dnd-kit/sortable`(不要用已停止维护的 react-beautiful-dnd)
- **数据持久化**:所有数据(音乐库、歌单结构、播放设置)保存为 JSON 文件,存放在 `app.getPath('userData')` 目录下,应用启动时自动加载,数据变更时自动保存(用防抖,500ms)
- **打包**:配置 electron-builder,能输出 Windows 安装包(NSIS)和便携版 exe

## 二、支持的音频格式

导入和播放需支持以下扩展名:
`.mp3` `.flac` `.wav` `.ogg` `.aac` `.m4a` `.mp4` `.wma` 说明:
- `.mp4` 按纯音频处理,忽略视频轨
- `.wma` 如果 Chromium 无法解码,导入时允许添加,播放失败时给出友好提示"该格式暂不支持播放",不要让应用崩溃
- 文件选择对话框和拖入窗口导入时,按上述扩展名过滤

## 三、核心功能需求

### 1. 音乐导入
- 支持三种导入方式:
  a. 菜单/按钮 →「导入文件」:系统文件选择框,可多选
  b. 菜单/按钮 →「导入文件夹」:递归扫描文件夹内所有支持格式的音频文件
  c. 直接把文件/文件夹拖入应用窗口
- 导入时用 music-metadata 解析元数据;解析失败时用文件名作为标题,不中断导入
- 导入过程显示进度(如"正在导入 34/120"),大批量导入不能卡死界面(分批异步处理)
- 音乐库按文件路径去重,重复导入的文件跳过
- 所有导入的歌曲进入"音乐库"(全部歌曲列表);歌曲只记录文件路径,不复制文件本体
- 如果播放时发现源文件已被删除/移动,提示"文件不存在",并在列表中将该歌曲标灰

### 2. 歌单系统(两层目录结构)
结构为:**歌单文件夹 → 歌单 → 歌曲**,即:
- 第一层:歌单文件夹(可创建、重命名、删除;删除时需确认,并连带删除其中的歌单)
- 第二层:歌单(可以直接放在根级,也可以放进某个歌单文件夹;可创建、重命名、删除)
- 歌单里存放歌曲的引用(同一首歌可以加入多个歌单)
- 左侧边栏用可折叠的树形结构展示:固定的"音乐库"入口 + 歌单文件夹(可展开/收起)+ 各歌单
- 侧边栏支持右键菜单:新建歌单、新建歌单文件夹、重命名、删除
- 歌单可以在文件夹之间拖动移动(拖进文件夹、拖出到根级)
- 向歌单添加歌曲的方式:
  a. 在音乐库或其他歌单中,右键歌曲 →「添加到歌单」→ 级联菜单选择目标歌单
  b. 把歌曲直接拖到左侧边栏的某个歌单上
- 从歌单移除歌曲(右键 → 从歌单中移除)只移除引用,不影响音乐库;从音乐库删除歌曲需确认,并同时从所有歌单移除(不删除磁盘文件)

### 3. 歌曲拖拽排序
- 在任意歌单内,歌曲行可以按住拖动,自由调整顺序,松手后立即生效并持久化保存
- 拖动过程要有清晰的视觉反馈(拖动中的行半透明、目标位置显示插入指示线)
- 支持先多选(Ctrl/Shift 点击)再整体拖动排序为加分项,至少要实现单曲拖动
- 音乐库(全部歌曲)默认按导入时间排序,支持点击列头按标题/艺术家/专辑/时长排序;歌单内则以手动拖拽顺序为准

### 4. 播放功能
- 播放控制:播放/暂停、上一首、下一首
- 进度条:显示当前时间/总时长,可点击和拖动跳转
- 音量控制:滑块调节 + 静音按钮,音量值持久化
- 播放模式四种,可循环切换:顺序播放、列表循环、单曲循环、随机播放
- 双击歌曲行 = 从该歌曲开始播放,播放队列即当前所在的歌单/音乐库列表
- 底部播放栏常驻,显示:当前歌曲封面(无封面时显示默认占位图)、标题、艺术家、播放控制、进度条、音量、播放模式按钮
- 当前正在播放的歌曲在列表中高亮显示
- 支持键盘快捷键:空格 = 播放/暂停,Ctrl+→ = 下一首,Ctrl+← = 上一首
- 支持系统媒体键(播放/暂停/上一曲/下一曲)为加分项

### 5. 搜索
- 顶部搜索框,在当前列表内按标题/艺术家/专辑实时过滤

## 四、界面布局与风格

```
┌─────────────────────────────────────────────┐
│  顶部栏:应用名 | 搜索框 | 导入按钮            │
├──────────┬──────────────────────────────────┤
│ 左侧边栏  │  主区域:歌曲列表                  │
│ ·音乐库  │  列:# | 封面 | 标题 | 艺术家 |     │
│ ▾文件夹A │      专辑 | 时长                  │
│   ·歌单1 │                                  │
│   ·歌单2 │                                  │
│ ·歌单3   │                                  │
├──────────┴──────────────────────────────────┤
│  底部播放栏:封面 歌名/歌手 | ◀ ▶∕❚❚ ▶ |      │
│  进度条 | 音量 | 播放模式                     │
└─────────────────────────────────────────────┘
```

- 深色主题为默认,配色现代简洁(类似 Spotify 的层次感,但不要抄袭其视觉资产)
- 界面语言为简体中文
- 窗口可调整大小,最小尺寸 960×640;侧边栏宽度可拖动调整

## 五、数据结构参考

```jsonc
{
  "tracks": {
    "<trackId>": {
      "id": "uuid",
      "path": "C:\\Music\\song.mp4",
      "title": "歌名",
      "artist": "歌手",
      "album": "专辑",
      "duration": 245.3,
      "coverDataUrl": null,   // 或缓存到磁盘的封面文件路径
      "addedAt": 1730000000
    }
  },
  "folders": [
    { "id": "uuid", "name": "华语", "playlistIds": ["p1", "p2"] }
  ],
  "playlists": {
    "p1": { "id": "p1", "name": "开车听", "trackIds": ["t3", "t1", "t7"] }
  },
  "rootPlaylistIds": ["p9"],   // 不在任何文件夹里的歌单
  "settings": { "volume": 0.8, "playMode": "order", "lastPlayedTrackId": null }
}
```

注意:歌单内顺序由 `trackIds` 数组顺序决定,拖拽排序 = 调整该数组。封面图片如果较大,不要直接存 JSON,提取后缓存为 userData 目录下的图片文件,JSON 里只存路径。

## 六、工程与质量要求

- 主进程/渲染进程职责分离:文件系统访问、目录扫描、元数据解析放在主进程(或通过 preload 暴露的 IPC),渲染进程只负责 UI 和播放;开启 contextIsolation,不要在渲染进程直接用 Node API
- 代码结构清晰,组件拆分合理,关键模块附简短注释
- 所有可能失败的操作(读文件、解析元数据、播放)都要有错误处理,任何单曲错误不能导致应用崩溃
- 提供 `README.md`:如何安装依赖、开发运行(`npm run dev`)、打包(`npm run build`)

## 七、验收标准(完成后请逐条自测)

1. 能通过按钮和拖入窗口两种方式导入 mp3/flac/m4a/mp4 文件,元数据和封面正确显示
2. 播放 mp4 文件时只有声音,界面上没有任何视频画面
3. 能新建歌单文件夹和歌单,歌单可放入文件夹,树形侧边栏展开/收起正常
4. 能把歌曲加入歌单,在歌单内拖动调整顺序,重启应用后顺序保持
5. 播放/暂停/上一首/下一首/进度拖动/音量/四种播放模式全部工作正常
6. 删除歌单、删除文件夹(含确认)、从歌单移除歌曲、重命名均正常
7. 重启应用后,音乐库、歌单结构、音量等全部恢复
8. 搜索框能实时过滤当前列表

## 八、开发顺序建议

请按以下顺序分步完成,每完成一步先自行验证再继续:
1. 搭建 Electron + React + Vite 项目骨架,窗口能启动
2. 实现导入(文件/文件夹/拖入)+ 元数据解析 + 音乐库列表展示
3. 实现底部播放栏与全部播放控制、播放模式
4. 实现歌单文件夹/歌单的树形侧边栏与增删改
5. 实现歌曲加入歌单、歌单内拖拽排序
6. 实现数据持久化与启动恢复
7. 实现搜索、快捷键、细节打磨
8. 配置 electron-builder 打包,输出 Windows 安装包

如对需求有疑问,先按本文档的明确描述实现;文档未提及的细节由你按主流音乐播放器的惯例合理决定,不必反复询问。
